# 版本管理

## 5.13 版本管理

- **綱要版本** 於記錄層級遵循 SemVer。每張收據上的 `schema_version: "1.0.0"` 允許向後相容讀取。
- **API 版本** 以 URL 為前綴（`/v1`、`/v2`）。兩個相鄰主要版本至少並行運作 12 個月。
- **分類法版本** 獨立於綱要版本。標準商品可從 `food.dairy.milk`（v1.0）移至 `food.dairy.fluid-milk`（v1.1），同時收據記錄綱要保持穩定。

---
