# 版本管理

## 5.13 版本管理

- **纲要版本** 于记录层级遵循 SemVer。每张收据上的 `schema_version: "1.0.0"` 允许向后相容读取。
- **API 版本** 以 URL 为前缀（`/v1`、`/v2`）。两个相邻主要版本至少并行运作 12 个月。
- **分类法版本** 独立于纲要版本。标准商品可从 `food.dairy.milk`（v1.0）移至 `food.dairy.fluid-milk`（v1.1），同时收据记录纲要保持稳定。

---
